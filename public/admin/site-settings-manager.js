/* Contact Information inside the existing Site Settings module. */
(()=>{
  const PATH='src/content/settings/general.json';
  const contactFields=[['church_name','Church Name'],['address','Address'],['pin_code','PIN Code (optional)'],['village_town','Village / Town'],['district','District'],['state','State'],['country','Country'],['phone_number','Phone Number'],['mobile_number','Mobile Number'],['whatsapp_number','WhatsApp Number'],['email','Email'],['website_url','Website URL'],['office_hours','Office Hours'],['google_maps_embed_url','Google Maps Embed URL']];
  const socialFields=[['facebook_url','Facebook'],['youtube_url','YouTube'],['instagram_url','Instagram'],['twitter_url','X (Twitter)'],['telegram_url','Telegram'],['whatsapp_channel_url','WhatsApp Channel']];
  window.SihphirSiteSettingsManager={async mount({api,esc,notification}){
    const host=document.querySelector('#view');document.querySelector('.top h1')?.replaceChildren('Site Settings');
    const response=await api('/api/admin/content?path='+encodeURIComponent(PATH)),data=JSON.parse(response.content);
    data.phone_number??=data.secretary_phone||'';data.email??=data.secretary_email||'';data.village_town??='Sihphir';data.district??='Aizawl';data.state??='Mizoram';data.country??='India';data.show_home_contact??=true;
    const fields=list=>list.map(([key,label])=>`<label class="${key==='address'||key==='google_maps_embed_url'?'wide':''}"><span>${label}</span><input name="${key}" value="${esc(data[key]||'')}"></label>`).join('');
    host.innerHTML=`<form id="contact-settings-form" class="site-control"><header class="panel site-control-head"><div><span>Website configuration</span><h2>Contact Information Management</h2><p>One source for the Home contact section and website Footer.</p></div><button class="gold">Save Settings</button></header><section class="panel site-settings-card"><header><div><h3>Contact Information</h3><p>Public church and office contact details.</p></div><label class="site-contact-toggle"><input name="show_home_contact" type="checkbox" ${data.show_home_contact!==false?'checked':''}><span>Show Contact Section on Home Page</span></label></header><div class="site-fields">${fields(contactFields)}</div></section><section class="panel site-settings-card"><header><div><h3>Social Media</h3><p>Official social profiles and communication channels.</p></div></header><div class="site-fields">${fields(socialFields)}</div></section><section class="panel site-settings-card legacy-contact"><header><div><h3>Existing Contact Person</h3><p>Preserved for the current Secretary label shown on the site.</p></div></header><div class="site-fields"><label><span>Secretary Name</span><input name="secretary_name" value="${esc(data.secretary_name||'')}"></label></div></section></form>`;
    document.querySelector('#contact-settings-form').onsubmit=async event=>{event.preventDefault();const values=new FormData(event.currentTarget);[...contactFields,...socialFields,['secretary_name']].forEach(([key])=>data[key]=String(values.get(key)||'').trim());data.show_home_contact=values.get('show_home_contact')==='on';data.secretary_phone=data.phone_number||data.mobile_number||data.secretary_phone||'';data.secretary_email=data.email||data.secretary_email||'';try{await api('/api/admin/content',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({path:PATH,content:JSON.stringify(data,null,2)+'\n',message:'admin: update contact information'})});notification('Contact information saved.');}catch(error){notification(error.message,'error');}};
  }};
})();
